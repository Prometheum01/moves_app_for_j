import 'package:flutter/material.dart';

class CustomBoxDecoration extends BoxDecoration {
  const CustomBoxDecoration.movesContainer(Color color, BorderRadius radius)
      : super(
          color: color,
          borderRadius: radius,
        );
}
